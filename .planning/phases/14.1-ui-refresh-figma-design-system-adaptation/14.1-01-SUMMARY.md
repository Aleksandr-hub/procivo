---
phase: 14.1-ui-refresh-figma-design-system-adaptation
plan: 01
subsystem: ui
tags: [css-tokens, sidebar, collapsible, composable, vue3, primevue, i18n]

requires:
  - phase: 14-infrastructure-security
    provides: "Existing layout shell (AppSidebar, AppTopbar, DashboardLayout)"
provides:
  - "CSS design tokens (colors, shadows, layout dimensions, transitions)"
  - "useCollapsibleSidebar composable with localStorage persistence"
  - "Collapsible sidebar with icon-only compact mode and tooltips"
  - "Topbar page title derived from route name"
affects: [14.1-02, 14.1-03]

tech-stack:
  added: []
  patterns: [design-tokens-css-variables, collapsible-sidebar-composable, page-title-route-mapping]

key-files:
  created:
    - frontend/src/shared/composables/useCollapsibleSidebar.ts
  modified:
    - frontend/src/assets/main.css
    - frontend/src/shared/components/AppSidebar.vue
    - frontend/src/shared/components/AppTopbar.vue
    - frontend/src/shared/layouts/DashboardLayout.vue
    - frontend/src/i18n/locales/en.json
    - frontend/src/i18n/locales/uk.json

key-decisions:
  - "Sidebar collapse/expand keys added to existing top-level 'sidebar' i18n object alongside task sidebar keys"
  - "Route-to-i18n mapping as static Record in AppTopbar — simple, no dynamic resolution needed"
  - "Module-level ref in useCollapsibleSidebar following useTheme pattern — no flash of wrong state"

patterns-established:
  - "Design tokens: CSS custom properties in :root with .app-dark overrides"
  - "Collapsible sidebar: useCollapsibleSidebar composable shared across AppSidebar and DashboardLayout"
  - "Page title: route.name -> i18n key mapping in AppTopbar"

requirements-completed: []

duration: 3min
completed: 2026-03-06
---

# Phase 14.1 Plan 01: Design Tokens, Collapsible Sidebar, Topbar Refresh Summary

**CSS design tokens with collapsible sidebar (250px/64px) using localStorage-persisted composable and topbar page title from route name**

## Performance

- **Duration:** 3 min
- **Started:** 2026-03-06T13:11:36Z
- **Completed:** 2026-03-06T13:14:28Z
- **Tasks:** 2
- **Files modified:** 7

## Accomplishments
- Design token CSS custom properties for colors, shadows, layout dimensions, and transitions
- Collapsible sidebar with smooth CSS transition between 250px and 64px icon-only mode
- PrimeVue tooltips on nav items in collapsed mode, logo compact "P" badge
- Topbar displays page title derived from route name via i18n, hamburger menu removed
- DashboardLayout margin adjusts to sidebar width via shared composable
- Sidebar state persists in localStorage across page refreshes

## Task Commits

Each task was committed atomically:

1. **Task 1: Design tokens + useCollapsibleSidebar composable** - `d5fe3eb` (feat)
2. **Task 2: Collapsible AppSidebar + Topbar page title + DashboardLayout wiring** - `fb1d942` (feat)

## Files Created/Modified
- `frontend/src/assets/main.css` - Design token CSS custom properties in :root and .app-dark
- `frontend/src/shared/composables/useCollapsibleSidebar.ts` - Sidebar collapse state with localStorage persistence
- `frontend/src/shared/components/AppSidebar.vue` - Collapsible sidebar with icon-only compact mode
- `frontend/src/shared/components/AppTopbar.vue` - Page title display, hamburger removed, compact spacing
- `frontend/src/shared/layouts/DashboardLayout.vue` - Wired to useCollapsibleSidebar, margin adjusts
- `frontend/src/i18n/locales/en.json` - sidebar.collapse/expand keys
- `frontend/src/i18n/locales/uk.json` - sidebar.collapse/expand keys

## Decisions Made
- Sidebar collapse/expand i18n keys added to existing top-level "sidebar" object (alongside task sidebar keys) to avoid namespace duplication
- Route-to-i18n mapping as static Record<string, string> in AppTopbar — simple lookup, no dynamic resolution needed
- Module-level ref in useCollapsibleSidebar follows useTheme pattern — localStorage read at module load avoids flash of wrong state

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Design tokens available for all subsequent plans (cards, badges, etc.)
- useCollapsibleSidebar composable ready for any component needing sidebar state
- Layout shell complete — Plan 02 (dashboard widgets) and Plan 03 (task board polish) can proceed

---
*Phase: 14.1-ui-refresh-figma-design-system-adaptation*
*Completed: 2026-03-06*

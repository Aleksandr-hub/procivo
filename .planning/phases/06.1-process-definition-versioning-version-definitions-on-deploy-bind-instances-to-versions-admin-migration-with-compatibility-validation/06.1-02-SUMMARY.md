---
phase: 06.1-process-definition-versioning
plan: 02
subsystem: workflow
tags: [vue3, primevue, pinia, i18n, drawer, migration-ui]

# Dependency graph
requires:
  - phase: 06.1-process-definition-versioning
    plan: 01
    provides: MigrateProcessInstancesHandler event-sourced, ListVersionsHandler with running_instance_count, ProcessDefinitionVersionDTO.runningInstanceCount
provides:
  - VersionHistoryDrawer.vue — PrimeVue Drawer with DataTable showing version list + running instance counts
  - processDefinitionApi.migrateInstances — POST /versions/{versionId}/migrate call
  - useProcessDefinitionStore.migrateInstances — action that calls API and refreshes versions
  - ProcessDefinitionVersionDTO.running_instance_count — frontend field for backend count
  - Version History button in ProcessDesignerPage header
  - 10 i18n keys per locale (uk.json + en.json) for migration UI
affects: [07-audit, future-version-ui-features]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "PrimeVue Drawer with v-model:visible via computed getter/setter for two-way binding"
    - "useConfirm() for migration confirmation — same pattern as ProcessInstanceDetailPage.vue"
    - "totalRunningOnOtherVersions() computed inline in confirmMigrate() for confirm message count"

key-files:
  created:
    - frontend/src/modules/workflow/components/VersionHistoryDrawer.vue
  modified:
    - frontend/src/modules/workflow/types/process-definition.types.ts
    - frontend/src/modules/workflow/api/process-definition.api.ts
    - frontend/src/modules/workflow/stores/process-definition.store.ts
    - frontend/src/modules/workflow/pages/ProcessDesignerPage.vue
    - frontend/src/i18n/locales/uk.json
    - frontend/src/i18n/locales/en.json

key-decisions:
  - "VersionHistoryDrawer uses computed drawerVisible with get/set for v-model:visible — standard PrimeVue pattern for overlay components"
  - "totalRunningOnOtherVersions() filters versions by id (not version_number) to correctly exclude the target version when computing the migration count"
  - "ConfirmDialog placed inside Drawer template (not globally) — scoped to migration workflow to avoid z-index conflicts"

patterns-established:
  - "Drawer overlay with v-model:visible: computed getter → props.visible, setter → emit('update:visible', val)"
  - "Migration confirmation: useConfirm.require() with acceptClass='p-button-warning' and executeMigrate() as accept callback"

requirements-completed: [VER-03]

# Metrics
duration: 5min
completed: 2026-03-01
---

# Phase 06.1 Plan 02: VersionHistoryDrawer Admin UI Summary

**PrimeVue Drawer with version history DataTable, running instance counts, and migration controls wired into ProcessDesignerPage**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-03-01T14:47:40Z
- **Completed:** 2026-03-01T14:52:25Z
- **Tasks:** 2
- **Files modified:** 7 (1 created, 6 modified)

## Accomplishments

- VersionHistoryDrawer.vue — 118-line component showing version list with version number (Tag badge), publish date, running instance count (warn/secondary severity), and Migrate button (disabled on current version)
- Migration confirmation via useConfirm with instance count in message, success/error toasts
- processDefinitionApi.migrateInstances added — POST /versions/{versionId}/migrate
- Store migrateInstances action — calls API, then fetchVersions to refresh running counts
- ProcessDesignerPage: "Version History" button in header + drawer wired with v-model:visible and @migrated handler
- 10 i18n keys added to both uk.json and en.json

## Task Commits

Each task was committed atomically:

1. **Task 1: Update frontend types, API client, and Pinia store for migration** - `53f6a7e` (feat)
2. **Task 2: Create VersionHistoryDrawer component and wire into ProcessDesignerPage** - `5c8b949` (feat)

**Plan metadata:** (docs commit follows)

## Files Created/Modified

- `frontend/src/modules/workflow/components/VersionHistoryDrawer.vue` — New: Drawer with DataTable, version columns, migrate button + confirm dialog
- `frontend/src/modules/workflow/types/process-definition.types.ts` — Added running_instance_count to ProcessDefinitionVersionDTO
- `frontend/src/modules/workflow/api/process-definition.api.ts` — Added migrateInstances method (POST /versions/{versionId}/migrate)
- `frontend/src/modules/workflow/stores/process-definition.store.ts` — Added migrateInstances action calling API + fetchVersions refresh
- `frontend/src/modules/workflow/pages/ProcessDesignerPage.vue` — Version History button + VersionHistoryDrawer wired with v-model and @migrated
- `frontend/src/i18n/locales/uk.json` — 10 migration UI keys added
- `frontend/src/i18n/locales/en.json` — 10 migration UI keys added

## Decisions Made

- Used computed getter/setter for v-model:visible in Drawer — standard PrimeVue two-way binding pattern, avoids direct prop mutation
- totalRunningOnOtherVersions() filters by version.id (not version_number) to correctly exclude the target version
- ConfirmDialog placed inside Drawer template — keeps migration confirmation scoped to the drawer context

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

Pre-existing TypeScript errors in unrelated files (organization module, tasks module) — these are out of scope and were not fixed. Workflow-specific TS compilation passes with zero errors.

## User Setup Required

None - no external service configuration required.

## Self-Check: PASSED

Files verified:
- `frontend/src/modules/workflow/components/VersionHistoryDrawer.vue` — FOUND (118 lines, > 80 minimum)
- `frontend/src/modules/workflow/types/process-definition.types.ts` — FOUND, contains `running_instance_count`
- `frontend/src/modules/workflow/api/process-definition.api.ts` — FOUND, contains `migrateInstances`
- `frontend/src/modules/workflow/stores/process-definition.store.ts` — FOUND, contains `migrateInstances`

Commits verified:
- `53f6a7e` — Task 1 commit found
- `5c8b949` — Task 2 commit found

Build verified: Vite production build passed (✓ built in 2.60s)

## Next Phase Readiness

- Admin migration UI complete — admins can now view version history and migrate running instances from the Designer
- Phase 06.1 complete (both plans done) — ready to proceed to next phase
- Backend API was built in Phase 06-02 and enhanced in Phase 06.1-01; this plan completes the frontend side

---
*Phase: 06.1-process-definition-versioning*
*Completed: 2026-03-01*

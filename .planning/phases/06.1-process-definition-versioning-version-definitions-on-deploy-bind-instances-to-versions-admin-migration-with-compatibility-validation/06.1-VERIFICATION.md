---
phase: 06.1-process-definition-versioning
verified: 2026-03-01T15:10:00Z
status: passed
score: 8/8 must-haves verified
re_verification: false
---

# Phase 06.1: Process Definition Versioning — Verification Report

**Phase Goal:** Version definitions on deploy, bind instances to versions, admin migration with compatibility validation
**Verified:** 2026-03-01T15:10:00Z
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | After migration, engine reconstitutes ProcessInstance with the new versionId — CompleteTaskNodeHandler loads the target version's snapshot, not the original | VERIFIED | `ProcessInstance.applyMigrated()` sets `$this->versionId = ProcessDefinitionVersionId::fromString($event->toVersionId)` (line 591-593). `MigrateProcessInstancesHandler` calls `migrateToVersion()` then `save()` which appends `ProcessInstanceMigratedEvent` to event store. |
| 2 | Migration validates that outgoing transition action_keys for active task nodes still exist in the target version | VERIFIED | `MigrateProcessInstancesHandler` lines 77-87: `ProcessGraph::fromSnapshot($snapshot)` then `$targetGraph->outgoingTransitions($token['node_id'])` checked for count > 0. Throws `DomainException` if zero outgoing transitions. |
| 3 | GET /versions returns running_instance_count per version so the admin UI can display migration impact | VERIFIED | `ListVersionsHandler` (lines 44-52) fetches `COUNT(*)` via DBAL `IN(:ids)` with `ArrayParameterType::STRING`. `ProcessDefinitionVersionDTO::jsonSerialize()` includes `running_instance_count`. |
| 4 | Admin can open a Version History drawer from the Designer page header | VERIFIED | `ProcessDesignerPage.vue` line 59: `<Button ... @click="showVersionHistory = true" />`. `VersionHistoryDrawer` wired at lines 69-74 with `v-model:visible="showVersionHistory"`. |
| 5 | Version History drawer shows version list with version number, publish date, and running instance count | VERIFIED | `VersionHistoryDrawer.vue` (119 lines): `DataTable` with `Column field="version_number"`, `Column field="published_at"`, `Column field="running_instance_count"` — all with slot templates. |
| 6 | Admin can trigger migration of running instances to a selected version with a confirmation dialog | VERIFIED | `confirmMigrate()` calls `useConfirm().require()` with message, header, acceptClass, acceptLabel. `executeMigrate()` calls `store.migrateInstances()` on accept. |
| 7 | After successful migration, version list refreshes and running instance counts update | VERIFIED | `store.migrateInstances()` calls `processDefinitionApi.migrateInstances()` then `fetchVersions()` — full refresh of version list including updated counts. |
| 8 | Migration errors are displayed to the admin | VERIFIED | `executeMigrate()` catch block: `toast.add({ severity: 'error', detail: getApiErrorMessage(error, t('workflow.migrateFailed')) })` |

**Score: 8/8 truths verified**

---

## Required Artifacts

### Plan 01 Artifacts (VER-01, VER-02)

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `backend/src/Workflow/Domain/Event/ProcessInstanceMigratedEvent.php` | Domain event for event-sourced migration | VERIFIED | Exists, 29 lines, implements `DomainEvent`, `eventName()` returns `'workflow.process_instance.migrated'` |
| `backend/src/Workflow/Domain/Entity/ProcessInstance.php` | migrateToVersion method + applyMigrated | VERIFIED | `migrateToVersion()` at line 323-335 with `isRunning()` guard. `applyMigrated()` at line 590-593. `apply()` match includes `ProcessInstanceMigratedEvent` arm at line 476. |
| `backend/src/Workflow/Infrastructure/EventStore/EventSerializer.php` | Serialization for ProcessInstanceMigratedEvent | VERIFIED | Serialize arm at lines 138-143, deserialize arm at lines 275-281 for `'workflow.process_instance.migrated'`. |
| `backend/src/Workflow/Infrastructure/ReadModel/ProcessInstanceProjection.php` | Read model projection handler for migration event | VERIFIED | `onProcessInstanceMigrated()` at lines 126-132, `#[AsMessageHandler(bus: 'event.bus')]`, updates `version_id` via `connection->update()`. |
| `backend/src/Workflow/Application/Command/MigrateProcessInstances/MigrateProcessInstancesHandler.php` | Event-sourced migration with action_key validation | VERIFIED | Uses `ProcessGraph::fromSnapshot` at line 54. Event-sourced migration loop at lines 97-105 (load, migrateToVersion, save). No direct SQL UPDATE on read model. |
| `backend/src/Workflow/Application/DTO/ProcessDefinitionVersionDTO.php` | Running instance count field | VERIFIED | `public int $runningInstanceCount = 0` in constructor (line 16). `fromEntity()` accepts optional count. `jsonSerialize()` includes `'running_instance_count'`. |

### Plan 02 Artifacts (VER-03)

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `frontend/src/modules/workflow/components/VersionHistoryDrawer.vue` | Version history panel with migration controls | VERIFIED | 119 lines (above 80 minimum). Drawer + DataTable + confirmMigrate + executeMigrate fully implemented. |
| `frontend/src/modules/workflow/types/process-definition.types.ts` | Updated ProcessDefinitionVersionDTO with running_instance_count | VERIFIED | Line 69: `running_instance_count: number` present in `ProcessDefinitionVersionDTO` interface. |
| `frontend/src/modules/workflow/api/process-definition.api.ts` | migrateInstances API method | VERIFIED | Lines 53-55: `migrateInstances(orgId, defId, versionId)` calls `httpClient.post(...)` with correct URL pattern `versions/${versionId}/migrate`. |
| `frontend/src/modules/workflow/stores/process-definition.store.ts` | migrateInstances store action | VERIFIED | Lines 72-75: `migrateInstances()` calls API then `fetchVersions()`. Exported in return object at line 91. |

---

## Key Link Verification

### Plan 01 Key Links

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `MigrateProcessInstancesHandler` | `EventSourcedProcessInstanceRepository` | `findById -> migrateToVersion -> save` | WIRED | Lines 98-104: `processInstanceRepository->findById(...)`, `instance->migrateToVersion(...)`, `processInstanceRepository->save(...)` — all three steps present. |
| `ProcessInstanceProjection::onProcessInstanceMigrated` | `workflow_process_instances_view` | UPDATE version_id on migration event | WIRED | Lines 129-131: `connection->update(self::TABLE, ['version_id' => $event->toVersionId], ['id' => $event->processInstanceId])` |
| `ListVersionsHandler` | `workflow_process_instances_view` | DBAL subquery for running instance count | WIRED | Lines 44-47: `connection->fetchAllAssociative('SELECT version_id, COUNT(*) as cnt ... WHERE version_id IN (:ids) AND status = :status GROUP BY version_id', ...)` |

### Plan 02 Key Links

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `VersionHistoryDrawer.vue` | `process-definition.store.ts` | `useProcessDefinitionStore` for versions and migrateInstances | WIRED | Line 24: `const store = useProcessDefinitionStore()`. Line 36: `store.versions`, line 56: `store.migrateInstances(...)`. |
| `ProcessDesignerPage.vue` | `VersionHistoryDrawer.vue` | v-model:visible toggle from header button | WIRED | Line 8 import, line 19 `showVersionHistory = ref(false)`, line 59 `@click="showVersionHistory = true"`, lines 69-74 `<VersionHistoryDrawer v-model:visible="showVersionHistory">`. |
| `process-definition.api.ts` | `POST /versions/{versionId}/migrate` | httpClient.post | WIRED | Line 54: `httpClient.post(\`${base(orgId)}/${defId}/versions/${versionId}/migrate\`)` — URL pattern matches backend route. |

---

## Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| VER-01 | 06.1-01 | Event-sourced migration — ProcessInstanceMigratedEvent updates aggregate versionId so engine uses target version for execution after migration | SATISFIED | `ProcessInstanceMigratedEvent` created and fully wired: domain event, EventSerializer, ProcessInstance.apply(), ProjectionHandler, MigrateProcessInstancesHandler all verified. |
| VER-02 | 06.1-01 | GET /versions returns running_instance_count per version; action_key compatibility validated before migration | SATISFIED | `ListVersionsHandler` DBAL COUNT query verified. `MigrateProcessInstancesHandler` action_key validation (`outgoingTransitions` count check) verified. `ProcessDefinitionVersionDTO` `running_instance_count` field verified in JSON serialization. |
| VER-03 | 06.1-02 | Admin can view version history and trigger migration from Designer page via VersionHistoryDrawer with confirmation dialog | SATISFIED | `VersionHistoryDrawer.vue` (119 lines) with DataTable, confirm dialog, success/error toasts verified. Wired into `ProcessDesignerPage.vue` header. 10 i18n keys verified in both uk.json and en.json. |

No orphaned requirements — all three VER-* requirements from REQUIREMENTS.md are claimed and satisfied.

---

## Anti-Patterns Found

No anti-patterns detected across all 10 phase files. No TODO/FIXME/placeholder comments, no empty implementations, no stub return values.

---

## Human Verification Required

### 1. Migration Flow End-to-End

**Test:** Start a process instance, then navigate to Designer, open Version History drawer, deploy a new version (v2), click "Migrate here" on v2, confirm the dialog.
**Expected:** Running instance count on old version decreases to 0 after migration; instance is now associated with v2 in the process instance list.
**Why human:** Requires live DB state, real event store replay, and visual confirmation of count updates in the drawer after migration.

### 2. Compatibility Rejection UI

**Test:** Start a process instance at a task node, then create a new version that removes the outgoing transition from that task node, then attempt migration via the drawer.
**Expected:** Error toast appears with the server's DomainException message (cannot migrate, no outgoing transitions).
**Why human:** Requires deliberate schema manipulation and visual verification of error toast content.

### 3. Drawer Visible State

**Test:** Open the Version History drawer and resize/scroll — verify 500px fixed width and layout correctness of DataTable columns.
**Expected:** Drawer opens from the right at 500px width, all three data columns readable without horizontal scroll.
**Why human:** Visual layout verification cannot be done via grep.

---

## Commit Verification

All 4 feature commits confirmed present:
- `ac7d309` — feat(06.1-01): ProcessInstanceMigratedEvent wired into aggregate, serializer, projection
- `2634e07` — feat(06.1-01): event-sourced migration, action_key validation, running instance count in versions API
- `53f6a7e` — feat(06.1-02): running_instance_count to DTO, migrateInstances to API and store
- `5c8b949` — feat(06.1-02): VersionHistoryDrawer with migration controls to Designer

---

## Summary

Phase 06.1 goal is fully achieved. All 8 observable truths are verified against actual code. The backend is correctly event-sourced: `ProcessInstanceMigratedEvent` is persisted to the event store via `MigrateProcessInstancesHandler`, which loads instances via `EventSourcedProcessInstanceRepository`, calls `migrateToVersion()` on the aggregate, and saves — causing the projection to update `version_id` in the read model via the event bus. No direct SQL UPDATE exists in the migration command handler. Compatibility validation uses `ProcessGraph::fromSnapshot` to reject migrations where task nodes at active tokens have no outgoing transitions in the target version. The frontend `VersionHistoryDrawer` is substantively implemented (119 lines), correctly uses `useProcessDefinitionStore`, renders `running_instance_count`, provides confirmation dialog before migration, and is wired into `ProcessDesignerPage` with a header button and `v-model:visible` pattern.

---

_Verified: 2026-03-01T15:10:00Z_
_Verifier: Claude (gsd-verifier)_

---
phase: 08-audit-logging
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - frontend/src/modules/audit/types/audit-log.types.ts
  - frontend/src/modules/audit/api/audit-log.api.ts
  - frontend/src/modules/audit/components/AuditLogTimeline.vue
  - frontend/src/modules/tasks/components/TaskDetailContent.vue
  - frontend/src/modules/workflow/pages/ProcessInstanceDetailPage.vue
  - frontend/src/modules/organization/pages/OrganizationDetailPage.vue
  - frontend/src/shared/i18n/locales/uk.json
  - frontend/src/shared/i18n/locales/en.json
autonomous: true
requirements:
  - AUDT-04

must_haves:
  truths:
    - "Task detail page shows an activity timeline with audit log entries for that task"
    - "Process instance detail page shows an activity timeline with audit log entries for that process instance"
    - "Organization detail page shows an activity timeline with audit log entries for that organization"
    - "Timeline entries display event type label, actor info, changes details, and formatted timestamp"
  artifacts:
    - path: "frontend/src/modules/audit/types/audit-log.types.ts"
      provides: "AuditLogDTO and AuditLogListResponse TypeScript types"
      contains: "AuditLogDTO"
    - path: "frontend/src/modules/audit/api/audit-log.api.ts"
      provides: "auditLogApi.list() calling GET /organizations/{orgId}/audit-log"
      contains: "auditLogApi"
    - path: "frontend/src/modules/audit/components/AuditLogTimeline.vue"
      provides: "Reusable timeline component with PrimeVue Timeline"
      contains: "<Timeline"
    - path: "frontend/src/modules/tasks/components/TaskDetailContent.vue"
      provides: "AuditLogTimeline integrated into task detail"
      contains: "AuditLogTimeline"
    - path: "frontend/src/modules/workflow/pages/ProcessInstanceDetailPage.vue"
      provides: "AuditLogTimeline integrated into process instance detail"
      contains: "AuditLogTimeline"
    - path: "frontend/src/modules/organization/pages/OrganizationDetailPage.vue"
      provides: "AuditLogTimeline integrated into organization detail"
      contains: "AuditLogTimeline"
  key_links:
    - from: "frontend/src/modules/audit/components/AuditLogTimeline.vue"
      to: "frontend/src/modules/audit/api/audit-log.api.ts"
      via: "API call in onMounted"
      pattern: "auditLogApi\\.list"
    - from: "frontend/src/modules/tasks/components/TaskDetailContent.vue"
      to: "frontend/src/modules/audit/components/AuditLogTimeline.vue"
      via: "component import and usage"
      pattern: "AuditLogTimeline"
---

<objective>
Build the frontend audit log timeline component and integrate it into task detail, process instance detail, and organization detail pages.

Purpose: AUDT-04 (activity timeline on detail pages). Each detail page gets a collapsible section showing recent audit activity for that entity, powered by the REST API from Plan 01.

Output: Audit module frontend (types, API client, AuditLogTimeline component), integrated into 3 detail pages, with full i18n support.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-audit-logging/08-RESEARCH.md
@.planning/phases/08-audit-logging/08-01-SUMMARY.md

Key existing files:
@frontend/src/modules/tasks/components/TaskDetailContent.vue
@frontend/src/modules/workflow/pages/ProcessInstanceDetailPage.vue
@frontend/src/modules/organization/pages/OrganizationDetailPage.vue

<interfaces>
<!-- Backend API contract from Plan 01 -->

GET /api/v1/organizations/{organizationId}/audit-log
Query params: entity_type, entity_id, actor_id, date_from, date_to, page, limit
Response:
```json
{
  "items": [
    {
      "id": "uuid",
      "event_type": "task_manager.task.status_changed",
      "actor_id": "user-uuid-or-null",
      "entity_type": "task",
      "entity_id": "task-uuid",
      "organization_id": "org-uuid",
      "changes": {"from": "open", "to": "in_progress"},
      "occurred_at": "2026-03-01T12:00:00+00:00"
    }
  ],
  "total": 42,
  "page": 1,
  "limit": 50
}
```

From frontend/src/shared/api/http-client.ts:
```typescript
// Axios instance with JWT auth and token refresh
import httpClient from '@/shared/api/http-client'
```

From frontend/src/shared/utils/date-format.ts:
```typescript
export function formatDateTime(date: string | Date): string
```

PrimeVue Timeline component (already in project, used in ProcessHistoryTimeline.vue):
```vue
<Timeline :value="entries" align="left">
  <template #marker="{ item }">...</template>
  <template #content="{ item }">...</template>
</Timeline>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create audit module frontend (types, API client, AuditLogTimeline component)</name>
  <files>
    frontend/src/modules/audit/types/audit-log.types.ts
    frontend/src/modules/audit/api/audit-log.api.ts
    frontend/src/modules/audit/components/AuditLogTimeline.vue
  </files>
  <action>
    **1. Create TypeScript types** (`frontend/src/modules/audit/types/audit-log.types.ts`):
    ```typescript
    export interface AuditLogDTO {
      id: string
      event_type: string
      actor_id: string | null
      entity_type: string
      entity_id: string
      organization_id: string | null
      changes: Record<string, unknown> | null
      occurred_at: string
    }

    export interface AuditLogListResponse {
      items: AuditLogDTO[]
      total: number
      page: number
      limit: number
    }

    export interface AuditLogListParams {
      entity_type?: string
      entity_id?: string
      actor_id?: string
      date_from?: string
      date_to?: string
      page?: number
      limit?: number
    }
    ```

    **2. Create API client** (`frontend/src/modules/audit/api/audit-log.api.ts`):
    - Import httpClient from `@/shared/api/http-client`
    - Export `auditLogApi` object with:
      - `list(orgId: string, params: AuditLogListParams = {}): Promise<AuditLogListResponse>` — calls `GET /organizations/${orgId}/audit-log` with params, returns `r.data`
    - Follow existing API client patterns (e.g., task.api.ts)

    **3. Create AuditLogTimeline component** (`frontend/src/modules/audit/components/AuditLogTimeline.vue`):
    - Props: `orgId: string`, `entityType: string`, `entityId: string`, `limit?: number` (default 20)
    - Uses PrimeVue `<Timeline>` component (import from 'primevue/timeline')
    - On mount, calls `auditLogApi.list(props.orgId, { entity_type: props.entityType, entity_id: props.entityId, limit: props.limit })`
    - Stores entries in `ref<AuditLogDTO[]>([])`, loading state in `ref(false)`
    - Event config map: Maps `event_type` string to `{ icon: string, color: string, labelKey: string }`:
      - `task_manager.task.created` → `{ icon: 'pi pi-plus', color: '#8B5CF6', labelKey: 'audit.taskCreated' }`
      - `task_manager.task.status_changed` → `{ icon: 'pi pi-refresh', color: '#3B82F6', labelKey: 'audit.taskStatusChanged' }`
      - `task_manager.task.assigned` → `{ icon: 'pi pi-user', color: '#3B82F6', labelKey: 'audit.taskAssigned' }`
      - `task_manager.task.claimed` → `{ icon: 'pi pi-check-circle', color: '#10B981', labelKey: 'audit.taskClaimed' }`
      - `task_manager.task.unclaimed` → `{ icon: 'pi pi-undo', color: '#F59E0B', labelKey: 'audit.taskUnclaimed' }`
      - `task_manager.task.deleted` → `{ icon: 'pi pi-trash', color: '#EF4444', labelKey: 'audit.taskDeleted' }`
      - `task_manager.comment.added` → `{ icon: 'pi pi-comment', color: '#6366F1', labelKey: 'audit.commentAdded' }`
      - `workflow.process.started` → `{ icon: 'pi pi-play', color: '#8B5CF6', labelKey: 'audit.processStarted' }`
      - `workflow.process.completed` → `{ icon: 'pi pi-flag', color: '#10B981', labelKey: 'audit.processCompleted' }`
      - `workflow.process.cancelled` → `{ icon: 'pi pi-times-circle', color: '#EF4444', labelKey: 'audit.processCancelled' }`
      - `identity.user.registered` → `{ icon: 'pi pi-user-plus', color: '#8B5CF6', labelKey: 'audit.userRegistered' }`
      - `identity.user.password_changed` → `{ icon: 'pi pi-lock', color: '#F59E0B', labelKey: 'audit.passwordChanged' }`
      - Default fallback: `{ icon: 'pi pi-circle', color: '#9CA3AF', labelKey: '' }` (show raw event_type)
    - Template structure:
      - Show `<ProgressSpinner>` while loading
      - Show "No activity" message when entries is empty and not loading (use i18n key `audit.noActivity`)
      - `<Timeline :value="entries" align="left">`
        - `#marker` slot: colored icon circle using event config
        - `#content` slot: event label (via `$t(config.labelKey)` or raw event_type), changes details (if changes is not null — show key: value pairs), formatted timestamp via `formatDateTime(item.occurred_at)`
    - CSS: Style marker icons with colored backgrounds (rounded circles), event details with muted text, timestamps small and gray. Follow the sidebar card pattern from TaskDetailSidebar (borderless, clean).
  </action>
  <verify>
    <automated>cd /Users/leleka/Projects/procivo/frontend && npx vue-tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>
    - AuditLogDTO, AuditLogListResponse, AuditLogListParams types exported
    - auditLogApi.list() function calls backend endpoint correctly
    - AuditLogTimeline.vue renders PrimeVue Timeline with event icons, labels, changes, and timestamps
    - Component handles loading and empty states
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate AuditLogTimeline into detail pages + add i18n keys</name>
  <files>
    frontend/src/modules/tasks/components/TaskDetailContent.vue
    frontend/src/modules/workflow/pages/ProcessInstanceDetailPage.vue
    frontend/src/modules/organization/pages/OrganizationDetailPage.vue
    frontend/src/shared/i18n/locales/uk.json
    frontend/src/shared/i18n/locales/en.json
  </files>
  <action>
    **1. Add i18n keys** to both `uk.json` and `en.json`:

    Under a new `"audit"` top-level key:
    ```json
    "audit": {
      "activityTimeline": "Activity Timeline" / "Хронологія подій",
      "noActivity": "No activity recorded" / "Подій не зафіксовано",
      "taskCreated": "Task created" / "Задачу створено",
      "taskStatusChanged": "Status changed" / "Статус змінено",
      "taskAssigned": "Task assigned" / "Задачу призначено",
      "taskClaimed": "Task claimed" / "Задачу взято в роботу",
      "taskUnclaimed": "Task unclaimed" / "Задачу повернено в пул",
      "taskDeleted": "Task deleted" / "Задачу видалено",
      "commentAdded": "Comment added" / "Додано коментар",
      "processStarted": "Process started" / "Процес запущено",
      "processCompleted": "Process completed" / "Процес завершено",
      "processCancelled": "Process cancelled" / "Процес скасовано",
      "userRegistered": "User registered" / "Користувача зареєстровано",
      "passwordChanged": "Password changed" / "Пароль змінено",
      "changesFrom": "from" / "з",
      "changesTo": "to" / "на"
    }
    ```

    **2. Integrate into TaskDetailContent.vue:**
    - Import `AuditLogTimeline` from `@/modules/audit/components/AuditLogTimeline.vue`
    - Add a new section after existing content (e.g., after comments or process context cards)
    - Wrap in a collapsible `<Panel>` (PrimeVue) or a simple `<div>` with a header:
      - Header: `{{ $t('audit.activityTimeline') }}`
      - Content: `<AuditLogTimeline :org-id="orgId" entity-type="task" :entity-id="taskId" :limit="20" />`
    - Get `orgId` from route params (already available as `route.params.orgId`)
    - Get `taskId` from the current task (already available in component)
    - Use `<Fieldset>` with `:legend="$t('audit.activityTimeline')"` and `:toggleable="true"` for collapsible behavior (PrimeVue Fieldset is already available in the project). Set `:collapsed="true"` by default to avoid API call on page load if using lazy loading. Alternatively, always load — the API call is fast.

    **3. Integrate into ProcessInstanceDetailPage.vue:**
    - Import `AuditLogTimeline`
    - Add a `<Fieldset>` section below the existing process instance details
    - Props: `orgId` from route, `entityType="process_instance"`, `entityId` from the loaded process instance
    - Header: `{{ $t('audit.activityTimeline') }}`

    **4. Integrate into OrganizationDetailPage.vue:**
    - Import `AuditLogTimeline`
    - Add a `<Fieldset>` section below existing organization details
    - For organization timeline, do NOT pass `entityId` — instead pass only `orgId` to show all organization-wide activity. The AuditLogTimeline component should handle the case where `entityId` is omitted by calling the API with only the orgId (all entities). Add optional `entityId` prop support to AuditLogTimeline if not already done. The API already filters by `organization_id` when no `entity_type` / `entity_id` is passed.
    - Update AuditLogTimeline props to make `entityType` and `entityId` optional. When both are omitted, call API with only orgId to get all activity for the organization.

    **5. Build verification:**
    - Run `npx vue-tsc --noEmit` — must pass
    - Run `npx vite build` — must produce a successful build
  </action>
  <verify>
    <automated>cd /Users/leleka/Projects/procivo/frontend && npx vue-tsc --noEmit 2>&1 | tail -20 && npx vite build 2>&1 | tail -10</automated>
  </verify>
  <done>
    - TaskDetailContent.vue includes AuditLogTimeline in a collapsible Fieldset
    - ProcessInstanceDetailPage.vue includes AuditLogTimeline in a collapsible Fieldset
    - OrganizationDetailPage.vue includes AuditLogTimeline showing org-wide activity
    - All 14 i18n keys present in both uk.json and en.json
    - TypeScript compiles, Vite build succeeds
  </done>
</task>

</tasks>

<verification>
1. `npx vue-tsc --noEmit` passes without type errors
2. `npx vite build` produces successful production build
3. AuditLogTimeline component renders PrimeVue Timeline with correct event icons and labels
4. TaskDetailContent, ProcessInstanceDetailPage, and OrganizationDetailPage each contain AuditLogTimeline
5. All i18n keys exist in both uk.json and en.json
</verification>

<success_criteria>
- Activity timeline is visible on task detail, process instance detail, and organization detail pages
- Timeline entries display event type (localized), changes (if any), and formatted timestamp
- Each timeline uses PrimeVue Timeline with colored icon markers matching event type
- Frontend builds and type-checks cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/08-audit-logging/08-02-SUMMARY.md`
</output>

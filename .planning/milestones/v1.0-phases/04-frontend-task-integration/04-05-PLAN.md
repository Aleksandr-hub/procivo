---
phase: 04-frontend-task-integration
plan: 05
type: execute
wave: 4
depends_on:
  - "04-02"
  - "04-04"
files_modified: []
autonomous: false
requirements:
  - FEND-01
  - FEND-02
  - FEND-03
  - FEND-04
  - FEND-05
  - FEND-06
  - FEND-07
  - FEND-08
  - FEND-09
  - FEND-10
  - FEND-11

must_haves:
  truths:
    - "All 11 FEND requirements verified through visual inspection and functional testing"
    - "Task detail full page renders correctly with 2-column layout"
    - "Workflow action submission works end-to-end (form -> validation -> API -> workflow advances)"
  artifacts: []
  key_links: []
---

<objective>
Visual and functional verification of the complete Phase 4 frontend implementation. Confirm all FEND-01 through FEND-11 requirements are met through the UI.

Purpose: This is the final quality gate before marking Phase 4 complete. The user inspects the actual UI to verify it matches the Figma prototype and all features work correctly.

Output: Confirmed working frontend, ready for Phase 5 (Designer Configuration).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification and lint check</name>
  <files></files>
  <action>
    Run the full frontend build and type check to ensure everything compiles:

    ```bash
    cd frontend && npx vue-tsc --noEmit && npm run build
    ```

    Also verify no linting errors:
    ```bash
    cd frontend && npm run lint 2>&1 | tail -20
    ```

    Fix any build or lint errors that arise.
  </action>
  <verify>
    <automated>cd /Users/leleka/Projects/procivo/frontend && npx vue-tsc --noEmit && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - TypeScript compiles with zero errors
    - Vite build succeeds
    - No lint errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and functional verification of all FEND requirements</name>
  <action>
    Present the complete Phase 4 implementation for user verification. The user inspects the UI in the browser to confirm all 11 FEND requirements are met.

    What was built:
    1. API fix: /execute-action renamed to /complete (FEND-05)
    2. ActionFormDialog: Zod validation, comment field, action-typed buttons (FEND-03, FEND-04)
    3. TaskCard: Process context badges, purple icon, Pool Task badge (FEND-07)
    4. 2-column layout: Main content + sidebar with info cards (FEND-01, FEND-02)
    5. PoolTaskBanner: Gradient banner with avatars, claim/unclaim (FEND-06)
    6. ProcessContextCard: Gradient card with process info, stage, progress (FEND-09)
    7. MyPathStepper: Custom stepper with green checks, pulsing current (FEND-10)
    8. ProcessDataCard: Expandable variable grid from previous stages
    9. Process navigation: "View Full Process" link opens in new tab (FEND-11)
    10. Process history: Timeline tab remains functional (FEND-08)

    How to verify — start frontend dev server: `cd frontend && npm run dev`

    1. Task List View (FEND-07): Navigate to Tasks page. Verify workflow tasks show purple icon, process context, Pool Task badge.
    2. Task Detail Full Page (FEND-01, FEND-02, FEND-06): Click expand. Verify 2-column layout, sidebar cards, pool banner, action buttons.
    3. Panel mode: Click task in list. Verify compact view without sidebar/stepper.
    4. Process Context (FEND-09, FEND-10, FEND-11): Verify gradient card, stepper, navigation link.
    5. Action Dialog (FEND-03, FEND-04, FEND-05): Test form validation and submission.
    6. Process History (FEND-08): Verify timeline tab.
  </action>
  <verify>
    <automated>cd /Users/leleka/Projects/procivo/frontend && npx vue-tsc --noEmit 2>&1 | tail -3</automated>
  </verify>
  <done>
    - User has visually confirmed all 11 FEND requirements in the browser
    - No critical UI issues found
    - Phase 4 approved for completion
  </done>
</task>

</tasks>

<verification>
All 11 FEND requirements tested through UI:
- FEND-01: Dynamic form fields render from form_schema
- FEND-02: Action buttons from form_schema.actions
- FEND-03: ActionFormDialog with fields + comment + assignment section
- FEND-04: Zod validation with inline errors
- FEND-05: API calls /complete endpoint
- FEND-06: Pool task banner with claim/unclaim
- FEND-07: Task card process context badges
- FEND-08: Process history timeline tab
- FEND-09: Process Context Card
- FEND-10: My Path Stepper
- FEND-11: Process navigation link
</verification>

<success_criteria>
- All visual elements match Figma prototype layout
- Workflow task actions submit successfully
- No JavaScript console errors during normal operation
- Responsive breakpoints work correctly
- User approves the implementation
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-task-integration/04-05-SUMMARY.md`
</output>

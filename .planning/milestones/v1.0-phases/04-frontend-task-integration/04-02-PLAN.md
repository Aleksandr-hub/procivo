---
phase: 04-frontend-task-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/modules/tasks/components/TaskCard.vue
  - frontend/src/i18n/locales/uk.json
  - frontend/src/i18n/locales/en.json
autonomous: true
requirements:
  - FEND-07

must_haves:
  truths:
    - "Workflow tasks in the list show a purple pi-sitemap icon instead of blue"
    - "Task cards display process context line: process_name -> node_name"
    - "Pool tasks show a 'Pool Task' badge in the card"
  artifacts:
    - path: "frontend/src/modules/tasks/components/TaskCard.vue"
      provides: "Enhanced task card with process context badges and pool task indicator"
      min_lines: 80
  key_links:
    - from: "frontend/src/modules/tasks/components/TaskCard.vue"
      to: "task.workflow_summary"
      via: "conditional rendering"
      pattern: "workflow_summary"
---

<objective>
Enhance TaskCard.vue with process context badges matching the Figma prototype (img.png): purple icon for workflow tasks, process name -> stage name line, Pool Task badge, and task ID prefix.

Purpose: Task list cards should clearly distinguish workflow tasks from regular tasks, showing the process context at a glance. This matches the Figma prototype list view (img.png) where each card shows "TASK-002 - Затвердження бюджету -> Розгляд керівниками" with priority/status badges.

Output: Enhanced TaskCard with full process context information.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types from task.types.ts -->
From frontend/src/modules/tasks/types/task.types.ts:
```typescript
export interface TaskWorkflowSummaryDTO {
  process_instance_id: string
  process_name: string
  node_name: string
  is_completed: boolean
}

export interface TaskDTO {
  id: string
  title: string
  description: string | null
  status: TaskStatus
  priority: TaskPriority
  dueDate: string | null
  assigneeId: string | null
  isPoolTask: boolean
  candidateRoleId: string | null
  candidateDepartmentId: string | null
  workflow_summary: TaskWorkflowSummaryDTO | null
  // ... other fields
}
```

Current TaskCard.vue already renders:
- task.title (one line)
- task.workflow_summary (process_name -> node_name) — basic, needs enhancement
- Tag with status
- Pool icon (pi-users) if pool task
- Due date
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance TaskCard with process context badges and visual distinction</name>
  <files>
    frontend/src/modules/tasks/components/TaskCard.vue
    frontend/src/i18n/locales/uk.json
    frontend/src/i18n/locales/en.json
  </files>
  <action>
    **Redesign TaskCard.vue to match Figma prototype (img.png):**

    The Figma shows task cards with this structure:
    ```
    [icon] TASK-XXX · Process Name → Stage Name       [Priority] [Status]
    Task Title
    Task description (truncated)
    [Assignee chip] [Label chips...]                   Deadline info
    ```

    **1. Add task type icon (left side):**
    - Workflow tasks: purple `pi-sitemap` icon in a small circle with purple background
    - Regular tasks: blue `pi-list` icon in a small circle with blue background
    - Use `task.workflow_summary !== null` to distinguish

    **2. Enhance the top meta line:**
    - Show task ID prefix if available (but backend doesn't return sequential IDs, so skip TASK-XXX numbering for now)
    - For workflow tasks, show: `process_name → node_name` as first line
    - Use interpunct separator (·) between elements

    **3. Add Pool Task badge:**
    - If `task.isPoolTask && !task.assigneeId`, show a small `Tag` with "Pool Task" text and info severity
    - Place near the status/priority badges

    **4. Improve visual hierarchy:**
    - Task title: bold, slightly larger
    - Process context line: smaller, muted color, with purple pi-sitemap icon
    - Description: one line truncated, muted text (add `task.description` display — currently not shown in card but Figma shows it)
    - Bottom row: status tag + pool badge + priority badge + due date

    **5. Priority badge styling:**
    - Show priority as colored Tag (existing) on the right side
    - Move status Tag to right side as well (matching Figma)

    **6. Add i18n keys:**
    - `tasks.poolTaskBadge`: "Pool Task" / "Pool Task" (same in both languages — it's a term)
    - Reuse existing keys for status/priority labels

    **7. Keep the `active` class and click behavior** unchanged.
  </action>
  <verify>
    <automated>cd /Users/leleka/Projects/procivo/frontend && npx vue-tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - Workflow tasks show purple pi-sitemap icon; regular tasks show blue pi-list icon
    - Process context line (process_name -> node_name) is visually distinct with muted smaller text
    - Pool Task badge (Tag) visible for unclaimed pool tasks
    - Task description shown (one line, truncated)
    - Priority and status badges on the right per Figma prototype
    - TypeScript compiles cleanly
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `npx vue-tsc --noEmit` — no errors
2. TaskCard renders workflow_summary data in process context line
3. Purple icon class applied for workflow tasks
4. Pool Task badge conditional on `isPoolTask && !assigneeId`
</verification>

<success_criteria>
- TaskCard clearly distinguishes workflow vs regular tasks visually
- Process context (process name + stage) visible on workflow task cards
- Pool tasks have a badge indicator
- Card matches Figma prototype layout (img.png)
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-task-integration/04-02-SUMMARY.md`
</output>
